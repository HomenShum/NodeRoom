node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.*
!.env.example
*.local
.convex/
.vscode/*
!.vscode/extensions.json
.idea/
.claude/
.nodeagent/
.solo/
.serena/
/btb-*.json
/btb-*.md
/btb-*.png
/docs/eval/gemini-media-judges/**/results/
.DS_Store
Thumbs.db
*.log
.vite/
coverage/

# Claude Design handoff bundle (reference only, not shipped)
.design-ref/

.vercel

# Playwright E2E outputs
test-results/
playwright-report/
playwright/.cache/

# local scratch
.tmp/
.tmp-qa/
.playwright-mcp/

# Proofloop run history/output (like .git objects) -- keep .proofloop/config.json tracked
.proofloop/runs/
.proofloop/goals/
.proofloop/setup/
.proofloop/live/
.proofloop/codegraph/
.proofloop/orchestrator/
.proofloop/prod-proxy-longrun/
.proofloop/runner/
.proofloop/workers/
.proofloop/memory/
.proofloop/memory.jsonl
.proofloop/live-memory.jsonl
# Legacy local promoted-regression mirror. Canonical promoted regressions are tracked in proofloop/regressions/.
.proofloop/regressions.json
docs/eval/scaffold-handoff.md
docs/eval/scaffold-report.json
noderoom-*.png
opendesign-*.png
docs/eval/finance-model-runs/
docs/eval/halo-runs/.active-run.json

# Walkthrough capture intermediates (regenerable: npm run walkthroughs)
remotion/public/frames/
NUL
docs/walkthroughs/*.mp4
remotion/public/audio/
remotion/public/video/
public/episodes/

# Playwright probe artifacts (screenshots + JSON, regenerable from test runs)
tests/.artifacts/

# Nested git repo reference (short-term fix; submodule would be the long-term fix)
skills/solo-founder-nodes/

# Local git worktree checkouts — see git worktree list
.worktrees/

.vercel

.vercel
.env*
design-reference/

# Always-On Rooms digest bootstrap outputs (local outbox journal + .eml drafts;
# reviewed by a human in Gmail — never committed, never sent from this repo)
data/digest/
