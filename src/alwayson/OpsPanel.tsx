/**
 * Always-On Rooms — owner ops panel (modes, caps, outbox state machine).
 * Ports design-reference/alwayson/ao-ops.jsx §5. Rendered by PublicRoomPage
 * under the Ops tab (demo/owner only). Implemented by the ops lane.
 */

export function AlwaysOnOpsPanel(): JSX.Element {
  return <div className="ao-panel" data-testid="ao-ops-panel" />;
}
