# CHANGE A: exported Account values

A reviewer sees five Account labels but receives five blank cells in the real XLSX. The3px box marks the current Account column, not a proposed screen redesign. Change only the two current canonical Q3 workbook field lookups; preserve visible table, computed variance, edited note, numeric/formula/format behavior, uploaded sheets and shared download transport. Captured on merged034a8f9 current source and installed reviewed dependencies at1440x960, actual memory journey, fallback fonts, no provider. DOM restored exactly after removing the overlay. The workbook is parsed directly; no native Excel claim.
