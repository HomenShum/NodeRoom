# NodeKit note-reference V2 proof

This packet is intentionally **Incomplete** rather than Verified.

- `before.txt` captures the prior optimistic V1 behavior.
- `after.txt` captures the V2 contract, test, build, and browser results.
- Current candidate pixels and receipts live under
  `evidence/nodekit-note-reference-surface/after/`.
- External NodeProof and Gemini judgment have not run, so this packet does not
  authorize a shipment or verification claim.

The release kill criterion is zero dual-truth escapes across twenty adversarial
mutations. The current focused suite satisfies that criterion.
