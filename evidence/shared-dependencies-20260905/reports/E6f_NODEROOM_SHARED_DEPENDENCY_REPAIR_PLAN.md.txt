# NODEROOM-SHARED-DEPENDENCY-01

Builder mode. A developer opening NodeRoom's reviewed mobile-export change cannot integrate it because two inherited dependency contracts fail in shared CI. Restore the declared immutable NodeSlide dependency set and install the first patched, internally consistent Tiptap cohort. Keep the published mobile commit and all existing checks unchanged.

**Status: concrete plan only; candidate remains clean and unchanged.** Parent review is the next implementation boundary. This fulfills the user's request to make each individual repository ready for developer/user handoff; it does not certify the entire repository or deployment.

## Exact current subject and observed causes

- Candidate: `D:/VSCode Projects/cafecorner_nodebench/nodebench_ai4/.portfolio-worktrees/noderoom-current-consumer-20260905`, branch `codex/noderoom-current-consumer-20260905`, HEAD `de9e02bc31c6bebb421a4889c1606a49f617f0f6`. The published mobile packet remains historical proof of its exact source/dependencies.
- [PR252](https://github.com/HomenShum/NodeRoom/pull/252) mounted job `101304111767` fails at `scripts/nodeslide-mounted-release-proof.ts:97`: missing root `@nodeslide/engine` pin. The next missing pin is `@nodeslide/react-headless`. Commit `d022d42d3c9b529779d6a922ae2e191f8bbb2c87` removed both during unused dependency cleanup; the package's current application imports did not reveal the release contract's direct-pin requirement. `vendor/nodeslide/README.md` and `release-lock.json` explicitly require all six runtime tarballs as exact root `file:` dependencies. The script enforces that contract before its real isolated installed-consumer/component/journey checks. No verifier change is warranted.
- All **11 vendored tarballs** still match manifest SHA-256 and npm SHA-512 integrity. Both manifest/install-upgrade receipts match release-lock hashes. Engine and react-headless already have exact 0.2.2 local-tarball lock rows through Convex/React dependencies. This is root dependency declaration drift, not a missing or damaged producer release.
- PR production-gate log `101304151727.log` stops at audit with **28 moderate** findings, all chained to `@tiptap/core` 3.26.0. The [reviewed advisory](https://github.com/advisories/GHSA-cp6q-959q-f8rh) identifies a prototype-manipulation path in `mergeAttributes` and fixes it in **3.30.4**; the [upstream release](https://github.com/ueberdosis/tiptap/releases/tag/v3.30.4) records that patch. This establishes a vulnerable dependency, not a demonstrated exploitable NodeRoom route. NodeRoom's fixed schema and sanitizer still require their actual scenario tests.
- Registry metadata for all **30 current Tiptap packages** confirms a 3.30.4 cohort. Its exact peer dependencies require matching core/pm versions. Six direct packages currently declare `^3.26.0`. The compatible cohort additionally requires ProseMirror model >=1.25.11 (current1.25.7) and view >=1.41.9 (current1.41.8). Other raised ProseMirror minimums and happy-dom's peer range are already met by the current lock. Therefore changing core alone would leave peer incompatibilities.
- Full inherited audit also has **one high development-only Browserslist finding** containing two advisory entries. That is outside the observed production audit repair; retain it explicitly and do not claim a clean full audit.
- Push commit-message job `101304111903` rejects the historical de9 message for missing `e2e`, `evidence`, `HANDOFF`, `src`, and `tests` areas. `scripts/commit-message.ts` checks every nonmerge commit in the first-parent range, using case-insensitive whole-word area names/aliases; package/lock both map to `package`, while HANDOFF/evidence have their literal names. Merge commits are skipped. The passing PR range does not retroactively validate de9. Preserve de9; make the next ordinary commit accurately name its own changed areas. No PR template exists in `.github`.

## Frozen scope

1. `package.json`: restore exactly `@nodeslide/engine: file:vendor/nodeslide/nodeslide-engine-0.2.2.tgz` and `@nodeslide/react-headless: file:vendor/nodeslide/nodeslide-react-headless-0.2.2.tgz`; raise the six existing Tiptap ranges to `^3.30.4`. No new package family, override, alias, script, runtime API, or policy knob.
2. `package-lock.json`: generate through targeted native npm resolution. Bind all six direct Tiptap requests to3.30.4 and keep the30-package cohort aligned; allow only the two NodeSlide root declarations, Tiptap package rows, and dependency rows whose changes are required by the new cohort. Inspect every changed row. Stop on unrelated churn, peer conflicts, unexpected package families, or need for force/legacy-peer flags. Record the actual selected ProseMirror versions and why they changed; minimum compatible versions1.25.11/1.41.9 are preferred.
3. `HANDOFF.md`: append the actual dependency/source/install/audit/mounted proof and remaining holds. Keep the reviewed mobile evidence's original dependency hashes and desktop null Account parity explicit.
4. New `evidence/shared-dependencies-20260905/` plus operator packet proof/receipt: exact before inputs/raw failures, registry/advisory references, command logs, after lock-delta and installed-identity checks. Use narrow `* -text` for raw evidence bytes. Do not rewrite `evidence/mobile-current-xlsx-20260905`.

All other authored files, existing tests, workflows, vendor tarballs/manifests/receipts, source app behavior, original environments, provider setup, and published refs remain protected. The seven-file mobile source work is already committed. Separate desktop Account repair stays paused until this integration closes.

INTENT: the root dependency declarations omit two runtime pins and lock a vulnerable editor cohort; the mounted/audit checks require six immutable pins and a patched dependency; the release README/lock and upstream package contracts require those exact pins and matching Tiptap peers. Restore those owning declarations rather than weaken their checks.

## One named proof and execution sequence after approval

1. Recheck clean HEAD/index/refs and preserve exact package, lock, HANDOFF, all vendor bytes, and built output before mutation. The read-only recon already binds23 relevant current files and unchanged Git metadata; expanded build-output custody happens before install/build. Keep raw current CI failures. Reproduce the old dependency-level prototype canary using the real installed core and DOM serializer in a packet-only scenario; label it a library boundary, not NodeRoom exploitation.
2. Use the native npm targeted package-lock-only operation with lifecycle scripts disabled and no broad audit-fix/update. Explicitly request the six direct Tiptap packages at3.30.4 and two existing file tarballs. Review package/lock delta before normal install. Preserve current lock and failed resolver outputs; do not hand-edit integrity or disguise incompatible peers.
3. Run normal `npm ci` from the candidate without environment files or special peer bypasses. Preserve exit/log; bind the installed package versions/integrities to the changed lock. Audit production at the unchanged `--omit=dev --audit-level=moderate` threshold and capture full audit separately. New findings remain real blockers, not exclusions.
4. Replay the unchanged `npm run nodeslide:mounted:release:proof`. It verifies all immutable bytes, creates a fresh isolated npm-installed consumer, imports actual React/Convex modules, runs component authorization/lifecycle proof, and runs four existing mounted journeys (memory, in-process Convex, isolated component, studio mount). No live Convex/provider/native Office is required. Preserve generated proof and its honest dirty candidate identity; it is not a committed release certificate.
5. Run the real installed advisory canary after update: ordinary attributes survive; own JSON `__proto__` cannot replace the result prototype or create inherited event-handler attributes through the DOM serializer. Include repeated/burst malicious+benign inputs with no cross-instance/global contamination. Use existing `notebookPaper.test.tsx`, native notebook ProseMirror tests, block/outline tests for actual editor rendering/sanitization, identity retention, sad/adversarial inputs, and existing burst/sustained behavior. Do not add a fake external exploit or weaken an existing test.
6. Run root and Convex TypeScript, normal build, and unchanged built-preview memory suite, plus the named notebook provenance browser test on the owned isolated app with source/installed/package bindings and saved pixels/console. Preserve natural mobile versus explicitly desktop route identity; no full visual grade follows from this dependency proof. Do not change UI/CSS in this slice.
7. Keep the repository's unchanged normal `floor` and `prod:gate` authoritative in shared Linux CI. The Windows full floor's native COM test is a separately diagnosed inherited hold, so do not execute COM in this slice or describe local selected checks as full-floor success. After local scoped proof/freeze, a fresh judge reviews bytes and evidence; parent owns ordinary conforming commit/push and final shared CI. The next commit body must name `package`, `HANDOFF`, and `evidence` if those are the actual areas. For example: `Repair package pins and document dependency proof in HANDOFF and evidence`. Check the actual staged area set before commit.
8. Freeze source/lock/index and receipt for nonauthor review before any commit/push. Record any later production-gate failure as a new observed gate; no assumption that removing the first failure makes the full gate pass.

## Limits and rollback

Dependency resolution compatibility and actual consumer success are **not yet tested**. No npm install/audit/build/product command was executed during this reconnaissance. Its only network reads were public advisory/release and exact registry metadata. There were no source/index/ref changes.

If resolution changes unrelated rows, fails the installed consumer, or breaks notebook behavior, preserve the attempt and restore only the explicitly owned package/lock candidate bytes from the bound baseline; do not reset the repository or delete worktrees. Install state is disposable candidate-local state, and reinstalling the preserved lock remains an explicit subsequent action. Retain original failed tests/CI and historic mobile proof.

The Windows native-repair deadline mismatch, full-audit development finding, nightly credential absence, desktop null Account labels, full visual/accessibility/performance grades, provider/native Office certification and production deployment remain open. There is no migration of old branches or blanket repository readiness claim.

## Reproducible read-only evidence

- `E6f-noderoom-shared-dependency-recon-01/recon-receipt.json`: exact head/index/refs/status bindings,23 protected file hashes, source unchanged, raw CI/baseline audit hashes.
- `E6f-noderoom-shared-dependency-recon-01/nodeslide-custody.json`:11 tarballs, two receipt hashes, actual archived package metadata, four present/two missing root pins.
- `E6f-noderoom-shared-dependency-recon-01/tiptap-cohort.json`:30 official registry records, exact peer/dependency ranges and required ProseMirror deltas.
- `E6f-noderoom-shared-dependency-recon-01/tiptap-advisory.json`, `tiptap-release.json`, `nodeslide-pin-removal.diff`: retained upstream/current-history evidence.
- `E6f-noderoom-shared-dependency-recon.py`: one-run read-only capture, no extraction/install/product execution; output directory intentionally refuses overwrite.
